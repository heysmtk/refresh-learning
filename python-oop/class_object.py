# class = je to šablona (plán, blueprint), podle které se dělají objekty
# object = konkrétní instance (výsledek) té oné šablony

# Proč to tak je?
# Bez tříd by musel být vše napsané "procedurálně", OOP mi
# umožnuje spojit data a funkce, které spolu souvisí do jednoho objektu!

class Dog:  # definice šablony
    pass

my_dog = Dog()  # vytvoření objektu z té šablony